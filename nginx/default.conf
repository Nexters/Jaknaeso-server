server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ =404;
    }

    location /v3/api-docs {
        alias /usr/share/nginx/html/openapi3.yaml;
        add_header Content-Type application/x-yaml;
    }
}